applications:
  csiSecretStore:
    enabled: true
  secretStoreCSIProviderGCP:
    enabled: true
  nginxIngress:
    enabled: true
    spec:
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
